import { useParams, useNavigate } from "react-router-dom";
import React from "react";

const EditFlock = () => {
  const { flockId } = useParams();

  return <div>EditFlock</div>;
};

export default EditFlock;
